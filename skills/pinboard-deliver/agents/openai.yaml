interface:
  display_name: "Pinboard: Deliver"
  short_description: "Deliver one accepted project checkpoint"
  default_prompt: "Use $pinboard-deliver to complete the active repository attempt's accepted checkpoint without widening its scope, leave the durable result, and report review delivery accurately."
