interface:
  display_name: "Pinboard: Deliver"
  short_description: "Deliver one accepted project attempt"
  default_prompt: "Use $pinboard-deliver to execute the accepted repository attempt without widening its scope, leave the durable result, and report review delivery accurately."
