interface:
  display_name: "Repository Work"
  short_description: "Coordinate one repository work ledger"
  default_prompt: "Use $repo-work to inspect the repository work state, show the legal next actions, and preview safe parallel work when requested."
