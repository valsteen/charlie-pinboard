interface:
  display_name: "Slop Cleanup"
  short_description: "Clean repository residue to a verified fixed point"
  default_prompt: "Use $slop-cleanup to trace, remove, and recursively recheck dead, test-only, and abandoned repository residue until no new cleanup candidate remains."
